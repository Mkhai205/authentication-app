# Email Configuration
# Choose one of the following EMAIL_SERVICE options:

# Option 1: Gmail with App Password (Easiest to set up)
EMAIL_SERVICE=gmail
USER_EMAIL=your-gmail@gmail.com
USER_PASSWORD=your-app-password-here
# Note: USER_PASSWORD should be a Gmail App Password, not your regular password
# To create an App Password: https://support.google.com/accounts/answer/185833

# Option 2: Gmail with OAuth2 (More secure, but complex setup)
# EMAIL_SERVICE=gmail-oauth2
# USER_EMAIL=your-gmail@gmail.com
# OAUTH_CLIENT_ID=your-oauth-client-id
# OAUTH_CLIENT_SECRET=your-oauth-client-secret
# OAUTH_REFRESH_TOKEN=your-oauth-refresh-token

# Option 3: Generic SMTP (for other providers like SendGrid, Mailgun, etc.)
# EMAIL_SERVICE=smtp
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# USER_EMAIL=your-email@example.com
# USER_PASSWORD=your-password

# Other existing environment variables...
JWT_SECRET=your-jwt-secret
CLIENT_URL=http://localhost:3000
